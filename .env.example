# DashScope/Bailian Configuration (Required)
DASHSCOPE_API_KEY=your_dashscope_api_key_here
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/api/v1

# Bailian App Configuration
# Option 1: Single app (legacy, for backward compatibility)
BAILIAN_APP_ID=your_bailian_app_id_here

# Option 2: Multiple apps mapping (recommended for v0.2.0+)
# Map models to app IDs with per-app default thinking params
# Supports both simple string format and extended object format:
# - Simple: BAILIAN_APP_MAPPING='{"bailian-app-1":"app-id-1","bailian-app-2":"app-id-2"}'
# - Extended: BAILIAN_APP_MAPPING='{"bailian-app-reasoning":{"app_id":"app-id-1","enable_thinking":true,"has_thoughts":false},"bailian-app-fast":{"app_id":"app-id-2","enable_thinking":false}}'
# Extended format allows each app to have different default enable_thinking and has_thoughts values
# Example explanation:
# - bailian-app-reasoning: Always enable thinking, but don't show reasoning by default
# - bailian-app-fast: Disable thinking by default for faster responses
# Note: These are defaults that can be overridden by request parameters (reasoning_effort, enable_thinking, has_thoughts)

# Reasoning Configuration
BAILIAN_REASONING_DELTA_MAX=180

# Server Configuration
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO
WORKERS=1

# CORS Settings
CORS_ALLOW_ORIGINS=["*"]
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=["*"]
CORS_ALLOW_HEADERS=["*"]

# Optional: OpenAI API Key (for testing/comparison)
# OPENAI_API_KEY=your_openai_api_key_here

# Optional: Rate Limiting
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_REQUESTS=100
# RATE_LIMIT_PERIOD=60

# Optional: API Authentication
# API_KEY_HEADER=X-API-Key
# API_KEYS=["key1", "key2"]